{
  "name": "pdf-extractor",
  "version": "0.1.0",
  "private": true,
  "description": "HTTP service that extracts structured data from PDFs. Input: PDF bytes. Output: a structured ExtractionResult with text, metadata, XMP, form fields, signature dictionaries, and DocuSign markers. Reusable by document-verifier and future services (contract-verifier, resume-parser, background-check).",
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "dev": "tsx watch --env-file=.env src/index.ts",
    "start": "node dist/index.js",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@career-ledger/pdf-signature-verifier": "*",
    "@hono/node-server": "^1.13.0",
    "hono": "^4.6.0",
    "pdf-lib": "^1.17.1",
    "unpdf": "^0.12.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^20.12.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=20"
  }
}
