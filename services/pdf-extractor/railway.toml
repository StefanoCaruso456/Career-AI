[build]
builder = "RAILPACK"
buildCommand = "npm install && npm run build:extractor"

[deploy]
startCommand = "npm --workspace services/pdf-extractor run start"
healthcheckPath = "/v1/health"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
