[build]
builder = "RAILPACK"
buildCommand = "npm install && npm run build:verifier"

[deploy]
startCommand = "npm --workspace services/document-verifier run start"
healthcheckPath = "/v1/health"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
