# api-gateway configuration

PORT=8080
NODE_ENV=development

# Shared secret Career-AI sends in the Authorization header for the demo.
# Replace with real session auth when identity-service is wired up.
GATEWAY_SHARED_SECRET=dev-career-ai-secret-change-me

# DID recorded as the issuer on every badge row. Pre-W3C this is just a
# string label on the badge; once we sign W3C VCs it must resolve to a key
# the gateway controls.
ISSUER_DID=did:web:career-ledger.example/issuer

# Postgres connection
DATABASE_URL=postgres://career_ledger:career_ledger_dev@localhost:5433/career_ledger

# Upstream PDF extraction service (kept separate for trust-boundary reasons —
# parses untrusted PDF binaries)
PDF_EXTRACTOR_URL=http://localhost:8788

# Content extraction mode for the in-process verifier:
#   heuristic — regex + substring matching (default, offline)
#   openai    — LLM-powered semantic check with strict offer-letter type
#               detection and recipient-name verification against the
#               uploader's account name
CONTENT_EXTRACTOR=heuristic

# Required only when CONTENT_EXTRACTOR=openai
# OPENAI_API_KEY=
# OPENAI_MODEL=gpt-5
